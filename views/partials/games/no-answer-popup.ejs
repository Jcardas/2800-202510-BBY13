<!-- No Answer Selected Popup -->
<div id="no-answer-popup" class="fixed inset-0 bg-black bg-opacity-40 flex items-center justify-center z-50 hidden">
    <div
        class="bg-white rounded-xl p-6 shadow-lg transform scale-95 opacity-0 transition-all duration-300 max-w-sm w-full text-center">
        <h2 class="text-red-600 text-2xl font-bold mb-2">Oops!</h2>
        <p class="text-gray-700 text-base">Please select an answer before submitting.</p>
        <button onclick="closeNoAnswerPopup()"
            class="mt-4 px-4 py-2 bg-red-500 text-white rounded hover:bg-red-600 transition">
            Got it
        </button>
    </div>
</div>