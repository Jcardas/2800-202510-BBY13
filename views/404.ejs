<%- include("partials/header") %>
    <!-- Navbar -->
    <%- include("partials/navbar") %>

    <!-- Accessibility Sliding Bar -->
    <%- include("partials/sliding_bar") %>

    <main class="container mx-auto mt-8 flex-grow">

        <!-- 404 Error Heading -->
        <h1 class="text-red-600 text-4xl font-bold text-left mb-12 ml-2">404 Error</h1>

        <!-- Error Message -->
        <p class="text-xl p-4">Page not found.</p>

        <!-- Error Image -->
        <div class="flex justify-center">
            <img 
                src="/img/unhappy-gma.png" 
                alt="Unhappy Grandma" 
                class="mx-auto max-w-full h-auto"
            >
        </div>

        <!-- Additional Message -->
        <p class="text-xl p-4">This definitely isn't your grandson! </p>
    </main>

<!-- Footer -->
<%- include("partials/footer") %>